<UserControl x:Class="CPAS.Views.UC_DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="1" Margin="5,7,5,7" Style="{StaticResource RoundTextBox}"  FontSize="20"/>
            <Button Background="Transparent" Margin="5"  Grid.Column="2" Style="{StaticResource RoundButtonStyle1}" BorderThickness="0">
                <Button.Content>
                    <Image Source="/CPAS;component/Images/search.png" Margin="7"/>
                </Button.Content>
            </Button>
            
        </Grid>
        <DataGrid Grid.Row="1" Margin="3"/>
    </Grid>
</UserControl>
